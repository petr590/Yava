Fixed a bug with infinities and NaN type float with the flag `-c=never'.

Fixed a bug with hex representation of negative numeric constants.

The inference of variable names is supplemented with methods such as xToY, length, size, count.
